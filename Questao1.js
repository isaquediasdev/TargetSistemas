// Código equivalente à questão 1 em JavaScript
let INDICE = 13, SOMA = 0, K = 0;

// Loop para somar os valores até que K seja menor que INDICE
while (K < INDICE) {
    K = K + 1;
    SOMA = SOMA + K;
}

// Exibir o valor final de SOMA
console.log(`O valor final de SOMA é: ${SOMA}`);
